name: school_schedule
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2
  flutter_draggable_gridview: ^0.0.13
  cupertino_icons: ^1.0.8
  image_picker: ^1.0.7
  path_provider_windows: ^2.1.2
  path_provider: ^2.1.5
  firebase_storage: ^12.4.3
  path: ^1.9.0
  animations: ^2.0.0 # Use the latest version available
  firebase_core: ^3.12.0
  firebase_auth: ^5.5.0
  cloud_firestore: 5.6.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/OritImg.jpg
